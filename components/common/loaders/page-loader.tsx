import LogoLoader from './logo-loader'

const PageLoader = () => {
    return (
        <div className="relative h-[90vh] flex flex-col items-center justify-center w-full">
            <LogoLoader />
        </div>
    )
}

export default PageLoader
